import React from 'react';
import classes from './calendar.module.css';

const Calendar = () => {
    return (
        <div>
            Calendar component
        </div>
    );
};

export default Calendar;
