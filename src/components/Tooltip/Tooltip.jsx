import React from 'react';

/** Компонент tooltip */
const Tooltip = ({ position, children }) => {
    return (
        <div>
            Компонент tooltip
        </div>
    );
}

export default Tooltip
